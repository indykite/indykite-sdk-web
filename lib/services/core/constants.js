/**
 * @type {{
 *   register: "register",
 *   login: "customer"
 * }}
 */
const flowTypes = {
  register: "register",
  login: "customer",
};

const colors = {
  error: "red",
  success: "#B6FB91",
  info: "white",
};

module.exports = {
  flowTypes,
  colors,
};
